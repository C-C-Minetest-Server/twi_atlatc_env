-- For textline ads, do NOT exceed 26 characters per line and two lines.
-- If you want to put texts in the centre, pad it with spaces ON BOTH SIDES.

F.textline_advertisments = {
    ["never_walk_on_tracks"] = {
        text = {
            "!! TRAINS WILL KILL YOU !!",
            "!! NEVER WALK ON TRACKS !!",
        },
        groups = {
            all = true,
        },
    },
}
